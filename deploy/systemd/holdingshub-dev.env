# HoldingsHub (dev) - systemd Environment File
# Place this file on your dev LXC at: /opt/holdingshub-dev/holdingshub-dev.env
# Then set EnvironmentFile=/opt/holdingshub-dev/holdingshub-dev.env in holdingshub-dev.service
# After edits: sudo systemctl daemon-reload; sudo systemctl restart holdingshub-dev

# App origin (used for redirects and metadataBase)
NEXT_PUBLIC_APP_ORIGIN=

# Supabase (dev project)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
# Some server code uses SUPABASE_URL, so mirror the public value
SUPABASE_URL=${NEXT_PUBLIC_SUPABASE_URL}
# Service role is needed only for server scripts (e.g. refreshPrices)
SUPABASE_SERVICE_ROLE_KEY=

# Optional APIs (needed only if you run scripts on this host)
TWELVE_DATA_API_KEY=
FINNHUB_API_KEY=
EXCHANGE_RATE_API_KEY=

# Safety flag used by scripts to guard against prod project misuse
ENVIRONMENT=development
