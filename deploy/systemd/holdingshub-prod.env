# HoldingsHub (prod) - systemd Environment File
# Place this file on your prod LXC at: /opt/holdingshub-prod/holdingshub-prod.env
# Then set EnvironmentFile=/opt/holdingshub-prod/holdingshub-prod.env in holdingshub-prod.service
# After edits: sudo systemctl daemon-reload; sudo systemctl restart holdingshub-prod

# App origin MUST match your public URL exactly (protocol + host + optional port)
# Example (no TLS, reverse proxy by IP): http://10.0.0.50:3000
# Example (domain behind TLS proxy): https://holdings.example.com
NEXT_PUBLIC_APP_ORIGIN=

# Supabase (production project)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
# Some server code uses SUPABASE_URL, so mirror the public value
SUPABASE_URL=${NEXT_PUBLIC_SUPABASE_URL}
# Service role needed for price refresh/audit scripts; keep secret
SUPABASE_SERVICE_ROLE_KEY=

# Optional APIs (only required if scripts run on this host)
TWELVE_DATA_API_KEY=
FINNHUB_API_KEY=
EXCHANGE_RATE_API_KEY=

# Safety flag used by scripts to allow prod-only operations
ENVIRONMENT=production
