{
	# Optional: enable telemetry off and admin off if preferred
	# admin off
}

# Environment: set SITE_DOMAIN in .env.docker
{$SITE_DOMAIN} {
	encode zstd gzip
	# Forward all traffic to the app service
	reverse_proxy app:3000
	# Optional: enable logging
	log {
		output stdout
		format console
	}
}
